{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "ProviderManifest",
  "description": "Static manifest describing what a provider plugin supports.\n\nReturned by both the one-shot `--manifest` CLI mode and the `initialize`\nJSON-RPC handshake. The fields here are the provider-specific overlay on\ntop of [`animus_plugin_protocol::PluginManifest`].",
  "type": "object",
  "properties": {
    "capabilities": {
      "description": "Capability flags.",
      "$ref": "#/$defs/ProviderCapabilities"
    },
    "description": {
      "description": "Human-readable description.",
      "type": "string"
    },
    "name": {
      "description": "Plugin name (e.g. `\"animus-provider-claude\"`).",
      "type": "string"
    },
    "supported_models": {
      "description": "Concrete model identifiers this provider can route to.\n\nExamples: `[\"claude-sonnet-4-6\", \"claude-opus-4-7\"]`,\n`[\"gpt-5\", \"gpt-5-mini\"]`. Hosts use this to validate the `model`\nfield of an [`AgentRunRequest`] before dispatching.",
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "tool": {
      "description": "Tool name passed through to the wrapped CLI (`\"claude\"`, `\"codex\"`,\n`\"gemini\"`, ...). Custom HTTP providers may set this to their plugin\nname.",
      "type": "string"
    },
    "version": {
      "description": "Plugin semver.",
      "type": "string"
    }
  },
  "required": [
    "name",
    "version",
    "description",
    "supported_models",
    "tool",
    "capabilities"
  ],
  "$defs": {
    "ProviderCapabilities": {
      "description": "Provider capability flags.",
      "type": "object",
      "properties": {
        "cancellation": {
          "description": "Provider supports `agent/cancel`.",
          "type": "boolean",
          "default": false
        },
        "mcp": {
          "description": "Provider supports MCP server bridging (i.e. accepts the\n`mcp_servers` field of [`AgentRunRequest`]).",
          "type": "boolean",
          "default": false
        },
        "resume": {
          "description": "Provider supports `agent/resume` against prior session ids.",
          "type": "boolean",
          "default": false
        },
        "streaming": {
          "description": "Provider emits `agent/output` deltas as the model produces them.",
          "type": "boolean",
          "default": false
        },
        "write_capable": {
          "description": "Provider can edit files in the working directory (vs. read-only\nresearch providers).",
          "type": "boolean",
          "default": false
        }
      }
    }
  }
}
